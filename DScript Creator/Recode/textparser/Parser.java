package textparser;

public class Parser {
	/**
	 * Parses a String of the new notation
	 * @param str
	 */
	public static Glyph parse(String str) {
		if(str == null) {
			return null;
		}
		
		Glyph in = new Glyph();
		return in;
	}
}
